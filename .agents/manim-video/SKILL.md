---
name: manim-video
description: "Manim CE animations: 3Blue1Brown math/algo videos."
version: 1.0.0
author: SHL0MS, Hermes Agent
license: MIT
platforms: [linux, macos, windows]
metadata:
  hermes:
    tags: [Manim, Animation, Math, Video]
    related_skills: []
---

# Manim Video Production Pipeline

## When to use

Use when users request: animated explanations, math animations, concept visualizations, algorithm walkthroughs, technical explainers, 3Blue1Brown style videos, or any programmatic animation with geometric/mathematical content. Creates 3Blue1Brown-style explainer videos, algorithm visualizations, equation derivations, architecture diagrams, and data stories using Manim Community Edition.

## Creative Standard

This is educational cinema. Every frame teaches. Every animation reveals structure.

**Before writing a single line of code**, articulate the narrative arc. What misconception does this correct? What is the "aha moment"? What visual story takes the viewer from confusion to understanding? The user's prompt is a starting point — interpret it with pedagogical ambition.

**Geometry before algebra.** Show the shape first, the equation second. Visual memory encodes faster than symbolic memory. When the viewer sees the geometric pattern before the formula, the equation feels earned.

**First-render excellence is non-negotiable.** The output must be visually clear and aesthetically cohesive without revision rounds. If something looks cluttered, poorly timed, or like "AI-generated slides," it is wrong.

**Opacity layering directs attention.** Never show everything at full brightness. Primary elements at 1.0, contextual elements at 0.4, structural elements (axes, grids) at 0.15. The brain processes visual salience in layers.

**Breathing room.** Every animation needs `self.wait()` after it. The viewer needs time to absorb what just appeared. Never rush from one animation to the next. A 2-second pause after a key reveal is never wasted.

**Cohesive visual language.** All scenes share a color palette, consistent typography sizing, matching animation speeds. A technically correct video where every scene uses random different colors is an aesthetic failure.

## Prerequisites

Run `scripts/setup.sh` to verify all dependencies. Requires: Python 3.10+, Manim Community Edition v0.20+ (`pip install manim`), LaTeX (`texlive-full` on Linux, `mactex` on macOS), and ffmpeg. Reference docs tested against Manim CE v0.20.1.

## Modes

| Mode | Input | Output | Reference |
|------|-------|--------|-----------|
| **Concept explainer** | Topic/concept | Animated explanation with geometric intuition | `references/scene-planning.md` |
| **Equation derivation** | Math expressions | Step-by-step animated proof | `references/equations.md` |
| **Algorithm visualization** | Algorithm description | Step-by-step execution with data structures | `references/graphs-and-data.md` |
| **Data story** | Data/metrics | Animated charts, comparisons, counters | `references/graphs-and-data.md` |
| **Architecture diagram** | System description | Components building up with connections | `references/mobjects.md` |
| **Paper explainer** | Research paper | Key findings and methods animated | `references/scene-planning.md` |
| **3D visualization** | 3D concept | Rotating surfaces, parametric curves, spatial geometry | `references/camera-and-3d.md` |

## Stack

Single Python script per project. No browser, no Node.js, no GPU required.

| Layer | Tool | Purpose |
|-------|------|---------|
| Core | Manim Community Edition | Scene rendering, animation engine |
| Math | LaTeX (texlive/MiKTeX) | Equation rendering via `MathTex` |
| Video I/O | ffmpeg | Scene stitching, format conversion, audio muxing |
| TTS | ElevenLabs / Qwen3-TTS (optional) | Narration voiceover |

## Pipeline

```
PLAN --> CODE --> RENDER --> STITCH --> AUDIO (optional) --> REVIEW
```

1. **PLAN** — Write `plan.md` with narrative arc, scene list, visual elements, color palette, voiceover script
2. **CODE** — Write `script.py` with one class per scene, each independently renderable
3. **RENDER** — `manim -ql script.py Scene1 Scene2 ...` for draft, `-qh` for production
4. **STITCH** — ffmpeg concat of scene clips into `final.mp4`
5. **AUDIO** (optional) — Add voiceover and/or background music via ffmpeg. See `references/rendering.md`
6. **REVIEW** — Render preview stills, verify against plan, adjust

## Project Structure

```
project-name/
  plan.md                # Narrative arc, scene breakdown
  script.py              # All scenes in one file
  concat.txt             # ffmpeg scene list
  final.mp4              # Stitched output
  media/                 # Auto-generated by Manim
    videos/script/480p15/
```

## Creative Direction

### Color Palettes

| Palette | Background | Primary | Secondary | Accent | Use case |
|---------|-----------|---------|-----------|--------|----------|
| **Classic 3B1B** | `#1C1C1C` | `#58C4DD` (BLUE) | `#83C167` (GREEN) | `#FFFF00` (YELLOW) | General math/CS |
| **Warm academic** | `#2D2B55` | `#FF6B6B` | `#FFD93D` | `#6BCB77` | Approachable |
| **Neon tech** | `#0A0A0A` | `#00F5FF` | `#FF00FF` | `#39FF14` | Systems, architecture |
| **Monochrome** | `#1A1A2E` | `#EAEAEA` | `#888888` | `#FFFFFF` | Minimalist |

### Animation Speed

| Context | run_time | self.wait() after |
|---------|----------|-------------------|
| Title/intro appear | 1.5s | 1.0s |
| Key equation reveal | 2.0s | 2.0s |
| Transform/morph | 1.5s | 1.5s |
| Supporting label | 0.8s | 0.5s |
| FadeOut cleanup | 0.5s | 0.3s |
| "Aha moment" reveal | 2.5s | 3.0s |

### Typography Scale

| Role | Font size | Usage |
|------|-----------|-------|
| Title | 48 | Scene titles, opening text |
| Heading | 36 | Section headers within a scene |
| Body | 30 | Explanatory text |
| Label | 24 | Annotations, axis labels |
| Caption | 20 | Subtitles, fine print |

### Fonts

**Use monospace fonts for all text.** Manim's Pango renderer produces broken kerning with proportional fonts at all sizes. See `references/visual-design.md` for full recommendations.

```python
MONO = "Menlo"  # define once at top of file

Text("Fourier Series", font_size=48, font=MONO, weight=BOLD)  # titles
Text("n=1: sin(x)", font_size=20, font=MONO)                  # labels
MathTex(r"\nabla L")                                            # math (uses LaTeX)
```

Minimum `font_size=18` for readability.

### Per-Scene Variation

Never use identical config for all scenes. For each scene:
- **Different dominant color** from the palette
- **Different layout** — don't always center everything
- **Different animation entry** — vary between Write, FadeIn, GrowFromCenter, Create
- **Different visual weight** — some scenes dense, others sparse

## Workflow

### Step 1: Plan (plan.md)

Before any code, write `plan.md`. See `references/scene-planning.md` for the comprehensive template.

### Step 2: Code (script.py)

One class per scene. Every scene is independently renderable.

```python
from manim import *

BG = "#1C1C1C"
PRIMARY = "#58C4DD"
SECONDARY = "#83C167"
ACCENT = "#FFFF00"
MONO = "Menlo"

class Scene1_Introduction(Scene):
    def construct(self):
        self.camera.background_color = BG
        title = Text("Why Does This Work?", font_size=48, color=PRIMARY, weight=BOLD, font=MONO)
        self.add_subcaption("Why does this work?", duration=2)
        self.play(Write(title), run_time=1.5)
        self.wait(1.0)
        self.play(FadeOut(title), run_time=0.5)
```

Key patterns:
- **Subtitles** on every animation: `self.add_subcaption("text", duration=N)` or `subcaption="text"` on `self.play()`
- **Shared color constants** at file top for cross-scene consistency
- **`self.camera.background_color`** set in every scene
- **Clean exits** — FadeOut all mobjects at scene end: `self.play(FadeOut(Group(*self.mobjects)))`

Step 3: Render
Bash
# Low quality draft (fast)
manim -ql script.py Scene1_Introduction Scene2_CoreConcept

# High quality production (1080p60)
manim -qh script.py Scene1_Introduction Scene2_CoreConcept
Step 4: Stitch
Bash
cat > concat.txt << 'EOF'
file 'media/videos/script/1080p60/Scene1_Introduction.mp4'
file 'media/videos/script/1080p60/Scene2_CoreConcept.mp4'
EOF

ffmpeg -y -f concat -safe 0 -i concat.txt -c copy final.mp4
Critical Rules & Craftsmanship Guidelines
Group vs VGroup for Scene Cleanouts:
Never run FadeOut(VGroup(*self.mobjects)). If self.mobjects contains camera frame objects, image mobjects, or non-VMobjects, VGroup will crash at runtime. Always use Group(*self.mobjects) for scene-wide transitions.

LaTeX Dependency Awareness:
MathTex and Tex require latex and dvipng/dvisvgm installed on the host system. If LaTeX is not installed, Manim will raise FileNotFoundError. Always provide fallbacks or use Text with unicode characters (e.g., Text("f(x) = x² + 2x + 1")) when system dependencies are uncertain.

Cinematic Camera Movement:
Use MovingCameraScene to zoom in on key details, pan across large diagrams, or reframe formulas dynamically. Camera movement creates engagement and guides visual focus naturally.

Subtitles & Audio Alignment:
Attach captions directly to animations using self.add_subcaption("...", duration=N) or the subcaption parameter in self.play().

References
Scene Planning
Contents: Narrative arcs, layout templates, scene transitions, planning template, SCAMPER & pedagogical techniques.

Granularity & Scene Structure
Pacing & Granularity: Break explainers into independent scenes (30–90 seconds each). Do not stuff 10 distinct concepts into one single Scene class.

3B1B Pattern: Grant Sanderson structures videos as modular scenes compiled together. Each scene introduces one sub-concept, builds a visual mental model, demonstrates it, and clears the screen.

Planning Template:

Hook / Misconception: What common intuition fails?

Geometric Intuition: Visual representation before equations.

Symbolic Representation: Introduce math notation tied to the geometry.

Synthesis / Conclusion: Combine insights into the "aha moment".

Pedagogical Strategies
SCAMPER Method:

Substitute: Replace abstract variables with visual geometric objects.

Combine: Overlay two concepts (e.g., secant lines turning into tangents).

Adapt: Shift perspective (e.g., viewing a 2D curve as a slice of 3D terrain).

Modify/Magnify: Zoom in on infinitesimal changes (
Δx→0
).

Put to another use: Show practical application before theoretical derivation.

Eliminate: Strip away non-essential clutter to highlight the core mechanism.

Reverse: Start with the end result and deconstruct it backwards.

Visual Design
Contents: 12 design principles, opacity layering, layout grid, typography, color palettes.

Color Palettes & Aesthetic Standards
Dark Mode Default:

Background: #1C1C1C or #0F0F0F (Soft dark background, never harsh pitch #000000).

Primary / Focus: #58C4DD (Cyan) or #68B5E5 (Sky Blue).

Secondary / Supporting: #83C167 (Green) or #FCA311 (Warm Amber).

Accent / Spotlight: #FFFF00 (Yellow) or #FF6B6B (Coral Red).

Neutral / Structural: #444444 for axes, #888888 for labels.

Opacity Hierarchy
Primary Focus (1.0 Opacity): Main curves, key equations, active data points.

Contextual Elements (0.4–0.6 Opacity): Secondary reference lines, previous states, helper labels.

Structural Background (0.15–0.25 Opacity): Grid lines, coordinate axes, background bounding boxes.

Layout & Spacing
Keep a 10% safe margin around all edges of the frame (7.1 units wide, 4.0 units high in Manim coordinates).

Never place text directly against screen edges.

Align text blocks left or center intentionally using align_to or next_to.

Animations
Contents: Core animations, rate functions, composition, .animate syntax, timing patterns.

Primary Animation Types
Creation: Write(text), Create(shape), DrawBorderThenFill(mobject), SpiralIn(mobject).

Transformation: Transform(A, B) (morphs A into B), ReplacementTransform(A, B) (replaces A with B in memory), TransformMatchingTex(eq1, eq2).

Attention: Indicate(mobject), Wiggle(mobject), Circumscribe(mobject), Flash(point).

Fading: FadeIn(mobject, shift=UP), FadeOut(mobject, shift=DOWN).

Rate Functions & Timing
linear: For continuous rotations, background scrolling, or uniform motion.

smooth (default): Standard ease-in-out for natural organic movement.

there_and_back: For temporary highlights or pulsing objects.

Golden Timing Rule: Every major animation should take 1.0 to 2.0 seconds. Follow every reveal with self.wait(1.0) to self.wait(2.0).

Python
# Modern .animate syntax
self.play(dot.animate.move_to(target_point), run_time=1.5, rate_func=smooth)
Mobjects
Contents: Text, shapes, VGroup/Group, positioning, styling, custom mobjects.

Primitive Mobjects
Shapes: Circle(), Square(), Rectangle(), Dot(), Line(), Arrow(), Polygon().

Text: Text("Hello World", font_size=36, color=WHITE).

Math: MathTex(r"e^{i\pi} + 1 = 0").

Grouping & Layout Containers
VGroup: Use ONLY for vector mobjects (VMobject). Supports styling methods like .set_color(), .set_stroke(), .set_fill().

Group: Container for arbitrary mobjects (mixing text, images, 3D objects, camera frames). Always use Group when grouping heterogeneous objects or running FadeOut(Group(*self.mobjects)).

Relative Positioning
Python
# Positioning methods
label.next_to(dot, UP, buff=0.2)
box.surround(text, buffer_factor=1.2)
element.align_to(reference_element, LEFT)
Equations
Contents: LaTeX in Manim, TransformMatchingTex, derivation patterns, step-by-step mathematical reveals.

Step-by-Step Derivations
Break equations into sub-strings using LaTeX index matching to enable automatic symbol tracking during transformations.

Python
# Split equation into matching parts
eq1 = MathTex(r"a^2", r"+", r"b^2", r"=", r"c^2")
eq2 = MathTex(r"c", r"=", r"\sqrt{a^2 + b^2}")

self.play(Write(eq1))
self.wait(1)
self.play(TransformMatchingTex(eq1, eq2))
Highlighting Equation Parts
Python
eq = MathTex(r"f(x) = ", r"\int_{a}^{b}", r"g(t) \, dt")
eq[1].set_color(YELLOW)  # Color integral sign specifically
No-LaTeX Workarounds
Contents: Full reference for LaTeX-free Manim, Unicode math character table, manual axis label patterns, capability matrix.

When LaTeX is not installed, use standard Text with Unicode symbols to render clear mathematical expressions without external system dependencies.

Unicode Math Character Table
Concept	Symbol	Unicode Example in Text
Greek Letters	
α,β,γ,π,θ,Δ,Σ
Text("α, β, γ, π, θ, Δ, Σ")
Operations	
×,÷,±,⋅, 

​
 
Text("a × b ± c · √x")
Calculus	
∂,∫,∞,∇,→
`Text("∫ f(x)dx = F(x)
Subscripts / Superscripts	
x 
1
​
 ,x 
2
​
 ,x 
2
 ,x 
3
 
Text("x₁ + x₂ = x² + x³")
Relations	
≤,≥,

=,≈,≡
Text("f(x) ≈ g(x) ≤ N")
Manual Axis Labels (LaTeX-Free)
Python
axes = Axes(
    x_range=[-3, 3, 1],
    y_range=[-2, 2, 1],
    axis_config={"include_numbers": False} # Disable LaTeX numbers
)

# Add text-based labels manually
x_label = Text("x", font_size=24).next_to(axes.x_axis, RIGHT)
y_label = Text("y", font_size=24).next_to(axes.y_axis, UP)
Graphs and Data
Contents: Axes, plotting, BarChart, animated data, algorithm visualization, function curves.

Coordinate Systems & Function Curves
Python
axes = Axes(
    x_range=[-2, 5, 1],
    y_range=[-1, 10, 2],
    x_length=8,
    y_length=5,
    axis_config={"color": BLUE_D}
)

# Plotting functions
graph = axes.plot(lambda x: x**2 - 2*x + 1, color=PRIMARY)
graph_label = axes.get_graph_label(graph, label="f(x) = x^2 - 2x + 1")

self.play(Create(axes), Create(graph))
Area Under Curve & Riemann Sums
Python
area = axes.get_riemann_rectangles(graph, x_range=[0, 3], dx=0.2, color=[BLUE, GREEN])
self.play(Create(area))
Camera and 3D
Contents: MovingCameraScene, ThreeDScene, 3D surfaces, camera control, spatial orientation.

Moving Camera Scene
Python
class ZoomInScene(MovingCameraScene):
    def construct(self):
        dot = Dot(POINT_ORIGIN, color=RED)
        self.add(dot)
        
        # Zoom camera into specific mobject
        self.play(
            self.camera.frame.animate.set_width(dot.width * 10).move_to(dot),
            run_time=2.0
        )
3D Surfaces & Lighting
Python
class Surface3DScene(ThreeDScene):
    def construct(self):
        axes = ThreeDAxes()
        surface = Surface(
            lambda u, v: np.array([u, v, np.sin(u) * np.cos(v)]),
            u_range=[-PI, PI],
            v_range=[-PI, PI]
        )
        self.set_camera_orientation(phi=75 * DEGREES, theta=-45 * DEGREES)
        self.play(Create(axes), Create(surface))
        self.begin_ambient_camera_rotation(rate=0.1)
        self.wait(3)
Rendering
Contents: CLI reference, quality presets, ffmpeg, voiceover workflow, GIF export.

Rendering Options & Flags
-ql: Low quality (480p, 15fps) - fast for development iterations.

-qm: Medium quality (720p, 30fps).

-qh: High quality (1080p, 60fps) - recommended for production.

-fk: 4K quality (2160p, 60fps).

-s: Save last frame as PNG picture.

--format=gif: Export directly as animated GIF.

Audio & Voiceover Integration
Bash
# Add narration audio track to rendered video
ffmpeg -i scene.mp4 -i voiceover.mp3 -c:v copy -c:a aac -map 0:v:0 -map 1:a:0 output_with_audio.mp4
Troubleshooting
Contents: LaTeX errors, animation errors, common mistakes, debugging techniques.

Issue / Error	Cause	Solution
FileNotFoundError: latex	System missing LaTeX distribution	Install TeX Live/MiKTeX OR switch to Text() with Unicode math symbols.
TypeError: VGroup can only contain VMobjects	Non-VMobject (Image, Group, Frame) passed to VGroup	Replace VGroup with Group.
Elements overlapping / messy layout	Missing explicit next_to or align_to	Define relative anchors for all new mobjects.
Sudden jump cuts	Forgetting self.play() or Transform	Smoothly transition objects using ReplacementTransform or FadeOut.
Text kerning broken	Fonts rendered improperly	Set standard system fonts (e.g., Arial, Helvetica, DejaVu Sans).
Updaters and Trackers
Contents: ValueTracker, add_updater, dynamic animations, complex linkages.

Value Tracker & Continuous Updates
Use ValueTracker to animate numerical parameters smoothly while updating dependent shapes or labels automatically.

Python
tracker = ValueTracker(0)

# Create dot that follows tracker value
dot = Dot()
dot.add_updater(lambda d: d.move_to(axes.c2p(tracker.get_value(), np.sin(tracker.get_value()))))

# Create dynamic label
label = AlwaysRedraw(lambda: DecimalNumber(tracker.get_value()).next_to(dot, UP))

self.add(dot, label)
self.play(tracker.animate.set_value(2 * PI), run_time=4, rate_func=linear)
Decorations
Contents: Backgrounds, grids, ambient movement, visual polish, glow effects.

Background Grids: Use NumberPlane(background_line_style={"stroke_opacity": 0.15}) to give abstract spaces structural grounding.

Focus Highlights: Add surrounding rounded boxes (SurroundingRectangle(mobject, color=YELLOW, buff=0.15)) to focus viewer attention.

Glow & Pulses: Use Indicate() or Flash() when key mathematical deductions occur.

Paper Explainer
Contents: Paper explainer video patterns, document animation, highlighting, academic paper breakdowns.

Import paper sections as structured cards or images.

Highlight key sentences using semi-transparent colored rectangles (Rectangle(fill_color=YELLOW, fill_opacity=0.3)).

Pan the camera (MovingCameraScene) from abstract 
→
 architecture diagrams 
→
 performance charts.

Production Quality
Contents: Craftsmanship, audio sync, polished visual aesthetics, review criteria.

Review Checklist Before Publishing
Pacing Check: Is there a 1–2s pause (self.wait()) after every major revelation?

Visual Contrast: Is primary content clearly distinguishable from background elements?

No Overlap: Do labels, formulas, or geometric shapes intersect unexpectedly?

Clean Exit: Does every scene clear its objects gracefully before ending?

Color Harmony: Is the color scheme consistent across all scenes in the project?

Animation Design Thinking
Contents: Pedagogy, narrative techniques, SCAMPER, conceptual framing.

Mental Model First: Design animations to build intuition, not to show off complex code.

Unpack Misconceptions: Start scenes by posing a question or highlighting a flawed assumption.

Visual Metaphors: Represent abstract math concepts using physical analogies (e.g., fluid dynamics, levers, geometric shadows, rotating epicycles).

Progressive Disclosure: Reveal information layer by layer rather than presenting full complex diagrams immediately.
"""

with open("skill.md", "w", encoding="utf-8") as f:
f.write(content)

print("skill.md created successfully")


```text?code_stdout&code_event_index=1
skill.md created successfully

Your Markdown file is ready

رمز MD
skill
 MD 
Manim Video Production Pipeline
Use when users request: animated explanations, math animations, concept visualizations, algorithm walkthroughs, technical explainers, 3Blue1Brown style videos, or any programmatic animation with geometric/mathematical content.

Creates 3Blue1Brown-style explainer videos, algorithm visualizations, equation derivations, architecture diagrams, and data stories using Manim Community Edition.

Creative Standard
This is educational cinema. Every frame teaches. Every animation reveals structure.

Narrative First: Before writing a single line of code, articulate the narrative arc. What misconception does this correct? What is the "aha moment"? What visual story takes the viewer from confusion to understanding? The user's prompt is a starting point — interpret it with pedagogical ambition.

Geometry Before Algebra: Show the shape first, the equation second. Visual memory encodes faster than symbolic memory. When the viewer sees the geometric pattern before the formula, the equation feels earned.

First-Render Excellence: The output must be visually clear and aesthetically cohesive without revision rounds. If something looks cluttered, poorly timed, or like "AI-generated slides," it is wrong.

Opacity Layering: Directs attention naturally. Never show everything at full brightness. Primary elements at 1.0, contextual elements at 0.4, structural elements (axes, grids) at 0.15. The brain processes visual salience in layers.

Breathing Room: Every animation needs self.wait() after it. The viewer needs time to absorb what just appeared. Never rush from one animation to the next. A 1.5 to 2-second pause after a key reveal is never wasted.

Cohesive Visual Language: All scenes share a color palette, consistent typography sizing, and matching animation speeds. A technically correct video where every scene uses random different colors is an aesthetic failure.

Typography & Kerning: Use standard system fonts with crisp layout alignment. When LaTeX is unavailable, fall back cleanly to Pango/Cairo Text with proper unicode symbols.

Production Pipeline & Workflow
Overview
PLAN: Write plan.md with narrative arc, scene list, visual elements, color palette, and voiceover script.

CODE: Write script.py with one class per scene, each independently renderable.

RENDER: Run manim -ql script.py SceneName for fast draft previews, and manim -qh for production quality.

STITCH: Combine scene clips into final.mp4 using ffmpeg concat.

REVIEW: Generate preview stills (-s), verify against design goals, and refine.

Step-by-Step Workflow
Step 1: Plan (plan.md)
Before writing code, define the structure in plan.md. Separate complex ideas into 4–16 focused scenes (30–90 seconds each) rather than crowding everything into a single overloaded scene.

Step 2: Code (script.py)
Write modular, clean Python code using Manim CE.

Python
from manim import *

# Shared Visual Constants
BG_COLOR = "#1C1C1C"
PRIMARY = "#58C4DD"     # Cyan / Main Focus
SECONDARY = "#83C167"   # Green / Supporting
ACCENT = "#FFFF00"      # Yellow / Highlights
TEXT_COLOR = "#ECEPE4"  # Soft White

class Scene1_Introduction(Scene):
    def construct(self):
        self.camera.background_color = BG_COLOR
        
        # Subtitles / Narratives
        title = Text("Why Does This Work?", font_size=44, color=PRIMARY, weight=BOLD)
        self.add_subcaption("Why does this work?", duration=2)
        
        self.play(Write(title), run_time=1.5)
        self.wait(1.0)
        
        # Clean exit - use Group, NOT VGroup (prevents crashes with non-VMobjects)
        self.play(FadeOut(Group(*self.mobjects)))
Step 3: Render
Bash
# Low quality draft (fast)
manim -ql script.py Scene1_Introduction Scene2_CoreConcept

# High quality production (1080p60)
manim -qh script.py Scene1_Introduction Scene2_CoreConcept
Step 4: Stitch
Bash
cat > concat.txt << 'EOF'
file 'media/videos/script/1080p60/Scene1_Introduction.mp4'
file 'media/videos/script/1080p60/Scene2_CoreConcept.mp4'
EOF

ffmpeg -y -f concat -safe 0 -i concat.txt -c copy final.mp4
Critical Rules & Craftsmanship Guidelines
Group vs VGroup for Scene Cleanouts:
Never run FadeOut(VGroup(*self.mobjects)). If self.mobjects contains camera frame objects, image mobjects, or non-VMobjects, VGroup will crash at runtime. Always use Group(*self.mobjects) for scene-wide transitions.

LaTeX Dependency Awareness:
MathTex and Tex require latex and dvipng/dvisvgm installed on the host system. If LaTeX is not installed, Manim will raise FileNotFoundError. Always provide fallbacks or use Text with unicode characters (e.g., Text("f(x) = x² + 2x + 1")) when system dependencies are uncertain.

Cinematic Camera Movement:
Use MovingCameraScene to zoom in on key details, pan across large diagrams, or reframe formulas dynamically. Camera movement creates engagement and guides visual focus naturally.

Subtitles & Audio Alignment:
Attach captions directly to animations using self.add_subcaption("...", duration=N) or the subcaption parameter in self.play().

References
Scene Planning
Contents: Narrative arcs, layout templates, scene transitions, planning template, SCAMPER & pedagogical techniques.

Granularity & Scene Structure
Pacing & Granularity: Break explainers into independent scenes (30–90 seconds each). Do not stuff 10 distinct concepts into one single Scene class.

3B1B Pattern: Grant Sanderson structures videos as modular scenes compiled together. Each scene introduces one sub-concept, builds a visual mental model, demonstrates it, and clears the screen.

Planning Template:

Hook / Misconception: What common intuition fails?

Geometric Intuition: Visual representation before equations.

Symbolic Representation: Introduce math notation tied to the geometry.

Synthesis / Conclusion: Combine insights into the "aha moment".

Pedagogical Strategies
SCAMPER Method:

Substitute: Replace abstract variables with visual geometric objects.

Combine: Overlay two concepts (e.g., secant lines turning into tangents).

Adapt: Shift perspective (e.g., viewing a 2D curve as a slice of 3D terrain).

Modify/Magnify: Zoom in on infinitesimal changes (
Δx→0
).

Put to another use: Show practical application before theoretical derivation.

Eliminate: Strip away non-essential clutter to highlight the core mechanism.

Reverse: Start with the end result and deconstruct it backwards.

Visual Design
Contents: 12 design principles, opacity layering, layout grid, typography, color palettes.

Color Palettes & Aesthetic Standards
Dark Mode Default:

Background: #1C1C1C or #0F0F0F (Soft dark background, never harsh pitch #000000).

Primary / Focus: #58C4DD (Cyan) or #68B5E5 (Sky Blue).

Secondary / Supporting: #83C167 (Green) or #FCA311 (Warm Amber).

Accent / Spotlight: #FFFF00 (Yellow) or #FF6B6B (Coral Red).

Neutral / Structural: #444444 for axes, #888888 for labels.

Opacity Hierarchy
Primary Focus (1.0 Opacity): Main curves, key equations, active data points.

Contextual Elements (0.4–0.6 Opacity): Secondary reference lines, previous states, helper labels.

Structural Background (0.15–0.25 Opacity): Grid lines, coordinate axes, background bounding boxes.

Layout & Spacing
Keep a 10% safe margin around all edges of the frame (7.1 units wide, 4.0 units high in Manim coordinates).

Never place text directly against screen edges.

Align text blocks left or center intentionally using align_to or next_to.

Animations
Contents: Core animations, rate functions, composition, .animate syntax, timing patterns.

Primary Animation Types
Creation: Write(text), Create(shape), DrawBorderThenFill(mobject), SpiralIn(mobject).

Transformation: Transform(A, B) (morphs A into B), ReplacementTransform(A, B) (replaces A with B in memory), TransformMatchingTex(eq1, eq2).

Attention: Indicate(mobject), Wiggle(mobject), Circumscribe(mobject), Flash(point).

Fading: FadeIn(mobject, shift=UP), FadeOut(mobject, shift=DOWN).

Rate Functions & Timing
linear: For continuous rotations, background scrolling, or uniform motion.

smooth (default): Standard ease-in-out for natural organic movement.

there_and_back: For temporary highlights or pulsing objects.

Golden Timing Rule: Every major animation should take 1.0 to 2.0 seconds. Follow every reveal with self.wait(1.0) to self.wait(2.0).

Python
# Modern .animate syntax
self.play(dot.animate.move_to(target_point), run_time=1.5, rate_func=smooth)
Mobjects
Contents: Text, shapes, VGroup/Group, positioning, styling, custom mobjects.

Primitive Mobjects
Shapes: Circle(), Square(), Rectangle(), Dot(), Line(), Arrow(), Polygon().

Text: Text("Hello World", font_size=36, color=WHITE).

Math: MathTex(r"e^{i\pi} + 1 = 0").

Grouping & Layout Containers
VGroup: Use ONLY for vector mobjects (VMobject). Supports styling methods like .set_color(), .set_stroke(), .set_fill().

Group: Container for arbitrary mobjects (mixing text, images, 3D objects, camera frames). Always use Group when grouping heterogeneous objects or running FadeOut(Group(*self.mobjects)).

Relative Positioning
Python
# Positioning methods
label.next_to(dot, UP, buff=0.2)
box.surround(text, buffer_factor=1.2)
element.align_to(reference_element, LEFT)
Equations
Contents: LaTeX in Manim, TransformMatchingTex, derivation patterns, step-by-step mathematical reveals.

Step-by-Step Derivations
Break equations into sub-strings using LaTeX index matching to enable automatic symbol tracking during transformations.

Python
# Split equation into matching parts
eq1 = MathTex(r"a^2", r"+", r"b^2", r"=", r"c^2")
eq2 = MathTex(r"c", r"=", r"\sqrt{a^2 + b^2}")

self.play(Write(eq1))
self.wait(1)
self.play(TransformMatchingTex(eq1, eq2))
Highlighting Equation Parts
Python
eq = MathTex(r"f(x) = ", r"\int_{a}^{b}", r"g(t) \, dt")
eq[1].set_color(YELLOW)  # Color integral sign specifically
No-LaTeX Workarounds
Contents: Full reference for LaTeX-free Manim, Unicode math character table, manual axis label patterns, capability matrix.

When LaTeX is not installed, use standard Text with Unicode symbols to render clear mathematical expressions without external system dependencies.

Unicode Math Character Table
Concept	Symbol	Unicode Example in Text
Greek Letters	
α,β,γ,π,θ,Δ,Σ
Text("α, β, γ, π, θ, Δ, Σ")
Operations	
×,÷,±,⋅, 

​
 
Text("a × b ± c · √x")
Calculus	
∂,∫,∞,∇,→
`Text("∫ f(x)dx = F(x)
Subscripts / Superscripts	
x 
1
​
 ,x 
2
​
 ,x 
2
 ,x 
3
 
Text("x₁ + x₂ = x² + x³")
Relations	
≤,≥,

=,≈,≡
Text("f(x) ≈ g(x) ≤ N")
Manual Axis Labels (LaTeX-Free)
Python
axes = Axes(
    x_range=[-3, 3, 1],
    y_range=[-2, 2, 1],
    axis_config={"include_numbers": False} # Disable LaTeX numbers
)

# Add text-based labels manually
x_label = Text("x", font_size=24).next_to(axes.x_axis, RIGHT)
y_label = Text("y", font_size=24).next_to(axes.y_axis, UP)
Graphs and Data
Contents: Axes, plotting, BarChart, animated data, algorithm visualization, function curves.

Coordinate Systems & Function Curves
Python
axes = Axes(
    x_range=[-2, 5, 1],
    y_range=[-1, 10, 2],
    x_length=8,
    y_length=5,
    axis_config={"color": BLUE_D}
)

# Plotting functions
graph = axes.plot(lambda x: x**2 - 2*x + 1, color=PRIMARY)
graph_label = axes.get_graph_label(graph, label="f(x) = x^2 - 2x + 1")

self.play(Create(axes), Create(graph))
Area Under Curve & Riemann Sums
Python
area = axes.get_riemann_rectangles(graph, x_range=[0, 3], dx=0.2, color=[BLUE, GREEN])
self.play(Create(area))
Camera and 3D
Contents: MovingCameraScene, ThreeDScene, 3D surfaces, camera control, spatial orientation.

Moving Camera Scene
Python
class ZoomInScene(MovingCameraScene):
    def construct(self):
        dot = Dot(POINT_ORIGIN, color=RED)
        self.add(dot)
        
        # Zoom camera into specific mobject
        self.play(
            self.camera.frame.animate.set_width(dot.width * 10).move_to(dot),
            run_time=2.0
        )
3D Surfaces & Lighting
Python
class Surface3DScene(ThreeDScene):
    def construct(self):
        axes = ThreeDAxes()
        surface = Surface(
            lambda u, v: np.array([u, v, np.sin(u) * np.cos(v)]),
            u_range=[-PI, PI],
            v_range=[-PI, PI]
        )
        self.set_camera_orientation(phi=75 * DEGREES, theta=-45 * DEGREES)
        self.play(Create(axes), Create(surface))
        self.begin_ambient_camera_rotation(rate=0.1)
        self.wait(3)
Rendering
Contents: CLI reference, quality presets, ffmpeg, voiceover workflow, GIF export.

Rendering Options & Flags
-ql: Low quality (480p, 15fps) - fast for development iterations.

-qm: Medium quality (720p, 30fps).

-qh: High quality (1080p, 60fps) - recommended for production.

-fk: 4K quality (2160p, 60fps).

-s: Save last frame as PNG picture.

--format=gif: Export directly as animated GIF.

Audio & Voiceover Integration
Bash
# Add narration audio track to rendered video
ffmpeg -i scene.mp4 -i voiceover.mp3 -c:v copy -c:a aac -map 0:v:0 -map 1:a:0 output_with_audio.mp4
Troubleshooting
Contents: LaTeX errors, animation errors, common mistakes, debugging techniques.

Issue / Error	Cause	Solution
FileNotFoundError: latex	System missing LaTeX distribution	Install TeX Live/MiKTeX OR switch to Text() with Unicode math symbols.
TypeError: VGroup can only contain VMobjects	Non-VMobject (Image, Group, Frame) passed to VGroup	Replace VGroup with Group.
Elements overlapping / messy layout	Missing explicit next_to or align_to	Define relative anchors for all new mobjects.
Sudden jump cuts	Forgetting self.play() or Transform	Smoothly transition objects using ReplacementTransform or FadeOut.
Text kerning broken	Fonts rendered improperly	Set standard system fonts (e.g., Arial, Helvetica, DejaVu Sans).
Updaters and Trackers
Contents: ValueTracker, add_updater, dynamic animations, complex linkages.

Value Tracker & Continuous Updates
Use ValueTracker to animate numerical parameters smoothly while updating dependent shapes or labels automatically.

Python
tracker = ValueTracker(0)

# Create dot that follows tracker value
dot = Dot()
dot.add_updater(lambda d: d.move_to(axes.c2p(tracker.get_value(), np.sin(tracker.get_value()))))

# Create dynamic label
label = AlwaysRedraw(lambda: DecimalNumber(tracker.get_value()).next_to(dot, UP))

self.add(dot, label)
self.play(tracker.animate.set_value(2 * PI), run_time=4, rate_func=linear)
Decorations
Contents: Backgrounds, grids, ambient movement, visual polish, glow effects.

Background Grids: Use NumberPlane(background_line_style={"stroke_opacity": 0.15}) to give abstract spaces structural grounding.

Focus Highlights: Add surrounding rounded boxes (SurroundingRectangle(mobject, color=YELLOW, buff=0.15)) to focus viewer attention.

Glow & Pulses: Use Indicate() or Flash() when key mathematical deductions occur.

Paper Explainer
Contents: Paper explainer video patterns, document animation, highlighting, academic paper breakdowns.

Import paper sections as structured cards or images.

Highlight key sentences using semi-transparent colored rectangles (Rectangle(fill_color=YELLOW, fill_opacity=0.3)).

Pan the camera (MovingCameraScene) from abstract 
→
 architecture diagrams 
→
 performance charts.

Production Quality
Contents: Craftsmanship, audio sync, polished visual aesthetics, review criteria.

Review Checklist Before Publishing
Pacing Check: Is there a 1–2s pause (self.wait()) after every major revelation?

Visual Contrast: Is primary content clearly distinguishable from background elements?

No Overlap: Do labels, formulas, or geometric shapes intersect unexpectedly?

Clean Exit: Does every scene clear its objects gracefully before ending?

Color Harmony: Is the color scheme consistent across all scenes in the project?

Animation Design Thinking
Contents: Pedagogy, narrative techniques, SCAMPER, conceptual framing.

Mental Model First: Design animations to build intuition, not to show off complex code.

Unpack Misconceptions: Start scenes by posing a question or highlighting a flawed assumption.

Visual Metaphors: Represent abstract math concepts using physical analogies (e.g., fluid dynamics, levers, geometric shadows, rotating epicycles).

Progressive Disclosure: Reveal information layer by layer rather than presenting full complex diagrams immediately.

‫
